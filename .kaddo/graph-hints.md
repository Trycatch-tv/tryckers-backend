# Kaddo Graph Hints

Generated by `kaddo graph export`. Suggestions only — Kaddo never edits your artifacts.

## Summary

- Scope: all — All supported Work Item statuses are included.
- Relationship quality: partial — Some active artifacts have missing relationship metadata.
- Nodes: 15
- Edges: 13
- Hints: 12

## Hints

###  — `knowledge/tech/decisions/ADR-001-s3-compatible-storage-driver.md`

Missing metadata:

- `code`

Suggested front matter:

```yaml
code:
  - <path/to/code>
```

Reason: This ADR defines technical decisions but does not declare which paths it governs.

###  — `knowledge/tech/decisions/ADR-002-relational-skills-and-tags.md`

Missing metadata:

- `code`

Suggested front matter:

```yaml
code:
  - <path/to/code>
```

Reason: This ADR defines technical decisions but does not declare which paths it governs.

###  — `knowledge/tech/decisions/ADR-003-refresh-token-rotation-and-storage.md`

Missing metadata:

- `code`

Suggested front matter:

```yaml
code:
  - <path/to/code>
```

Reason: This ADR defines technical decisions but does not declare which paths it governs.

###  — `knowledge/tech/decisions/ADR-004-database-migrations-engine.md`

Missing metadata:

- `code`

Suggested front matter:

```yaml
code:
  - <path/to/code>
```

Reason: This ADR defines technical decisions but does not declare which paths it governs.

### Capability Domains

Missing metadata:

- `work-item`

Reason: This capability is declared but no Work Item references it yet.

### Domain: Identity and Access Management (IAM)

Missing metadata:

- `work-item`

Reason: This capability is declared but no Work Item references it yet.

### Domain: Professional Identity and Profiles

Missing metadata:

- `work-item`

Reason: This capability is declared but no Work Item references it yet.

### Domain: Media and Storage

Missing metadata:

- `work-item`

Reason: This capability is declared but no Work Item references it yet.

### Domain: Community Discussions and Content (Posts and Comments)

Missing metadata:

- `work-item`

Reason: This capability is declared but no Work Item references it yet.

### Domain: Social Signals and Curation (Votes and Billboard)

Missing metadata:

- `work-item`

Reason: This capability is declared but no Work Item references it yet.

### Capability Gaps

Missing metadata:

- `work-item`

Reason: This capability is declared but no Work Item references it yet.

### Roadmap Candidate Signals

Missing metadata:

- `work-item`

Reason: This capability is declared but no Work Item references it yet.

> Use the `graph-agent` to turn these hints into precise front matter — you confirm and apply.
